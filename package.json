{
  "name": "workspace",
  "version": "1.0.0",
  "description": "This project sets up an automated pipeline for converting videos to audio and then transcribing them using whisper.cpp on a Synology NAS using Docker and Portainer.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "docker-compose": "^1.2.0"
  }
}
